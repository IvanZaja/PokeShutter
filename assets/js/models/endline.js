class endline {

    constructor(ctx, x, y) {
        this.ctx = ctx;
        this.x = x;
        this.y = y;

        this.w = Math.ceil(360);
        this.h = Math.ceil(10);
    }

    draw() {
        
  }
}
